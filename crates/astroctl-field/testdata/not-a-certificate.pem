This file is deliberately not a certificate.

An operator points `server.tls.cert_path` at the wrong file more often than they point it at a
corrupted one -- a README, a CSR, a chain that was truncated by a failed copy. The node must
refuse to start and name the path, rather than serve plain HTTP and leave the PWA silently
uninstallable. This fixture is what asserts that.
