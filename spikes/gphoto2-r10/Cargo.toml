[package]
name = "gphoto2-r10-spike"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"

# Deliberately NOT a workspace member (M2-T01: "scratch, not in workspace").
# This is a report task; nothing here is production code.
[workspace]

[dependencies]
gphoto2 = "3.4.1"
rawler = "0.7.2"
